import React from 'react';
import styled from 'styled-components';
import PropTypes from 'prop-types';

// ongoing
const ArrowDownside = (props) => {
  return <React.Fragment></React.Fragment>;
};

export default ArrowDownside;
