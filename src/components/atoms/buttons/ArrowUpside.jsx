import React from 'react';
import styled from 'styled-components';
import PropTypes from 'prop-types';

// ongoing
const ArrowUpside = (props) => {
  return <React.Fragment></React.Fragment>;
};

export default ArrowUpside;
